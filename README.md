# xeBuild freeBOOT 0.10

This is the rewritten version of freeBOOT; Wrote for fbBuild / xeBuild by [cOz].


## Build

`cmake -B build -DXENON_PREFIX=[PATH_TO_DEVKITXENON_BIN]/xenon-`

`cmake --build build`